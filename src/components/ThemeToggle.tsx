"use client";

// Theme toggle is disabled for the Konnichiwa design system
// which uses a fixed dark theme. This component is kept for
// compatibility but returns null.

export function ThemeToggle() {
  return null;
}
